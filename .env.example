# Sao chép file này thành .env rồi điền giá trị thật.
# Không commit file .env lên GitHub.

# API key Gemini — lấy tại https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-google-ai-studio-key-here

# Giữ = 0 nếu dùng Google AI Studio (không dùng Vertex AI)
GOOGLE_GENAI_USE_VERTEXAI=0

# Free-tier friendly model-call controls. Tests can set delay to 0 with mocks.
MODEL_REQUEST_DELAY_SECONDS=5
MODEL_MAX_RETRIES=3
MODEL_MAX_CONCURRENCY=1
